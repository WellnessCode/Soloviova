{"id":"tilda_bot_001","cmd":"cat > /tmp/read_tilda.py << 'PYEOF'\nimport asyncio, os\nfrom dotenv import load_dotenv\nfrom telethon import TelegramClient\nfrom telethon.tl.functions.contacts import ResolveUsernameRequest\n\nload_dotenv('/root/my-bot/.env')\napi_id = int(os.getenv('TELEGRAM_API_ID'))\napi_hash = os.getenv('TELEGRAM_API_HASH')\n\nasync def main():\n    client = TelegramClient('/root/my-bot/user_session', api_id, api_hash)\n    await client.start()\n    me = await client.get_me()\n    print(f'Акаунт: {me.first_name} @{me.username}')\n    \n    # Знаходимо TildaFormsBot\n    bot = await client.get_entity('TildaFormsBot')\n    print(f'Bot: {bot.first_name} id={bot.id}')\n    \n    # Читаємо останні 500 повідомлень\n    found = []\n    async for msg in client.iter_messages(bot, limit=500):\n        if msg.text and ('немає' in msg.text or 'Ні' in msg.text):\n            found.append(msg.text[:300])\n    \n    print(f'\\nЗнайдено повідомлень з Ні/немає: {len(found)}')\n    for i, txt in enumerate(found[:10], 1):\n        print(f'\\n--- {i} ---')\n        print(txt)\n    \n    await client.disconnect()\n\nasyncio.run(main())\nPYEOF\n/root/my-bot/venv/bin/python3 /tmp/read_tilda.py 2>&1 | head -100"}